node_modules
dist
dist-ssr
*.local

.DS_Store
.env
.env.local

.vscode/*
!.vscode/extensions.json
.idea

# Supabase CLI local cache/session state (project ref, pooler host, etc.)
# — machine-local, not project source.
supabase/.temp
